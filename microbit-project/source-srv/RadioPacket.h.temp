#pragma once
#include "MicroBit.h"

class RadioPacket{
    public: 
        RadioPacket(PacketBuffer p);
        ~RadioPacket();
        int getError();
    
    private:
        void setErrorCode(int code);
        PacketBuffer buffer;
        char opcode;
        char dataSize[2];
        char idSource[2];
        char idDest[2];
        char *data;
        char checksum;
        int errornb;
};